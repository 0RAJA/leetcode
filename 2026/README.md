> [youngyangyang04/leetcode-master: 《代码随想录》LeetCode 刷题攻略：200道经典题目刷题顺序，共60w字的详细图解，视频难点剖析，50余张思维导图，支持C++，Java，Python，Go，JavaScript等多语言版本，从此算法学习不再迷茫！🔥🔥 来看看，你会发现相见恨晚！🚀](https://github.com/youngyangyang04/leetcode-master?tab=readme-ov-file)

# 目录约定

- 每道题独立目录、独立 Go package。
- 目录名和 package 名使用小写标准名，例如 `intervalsum`。
- 题解文件统一命名为 `solution.go`，测试文件统一命名为 `solution_test.go`。
- 临时验证或 scratch demo 不放在 `2026/` 根目录，避免进入标准题解包。

# 数组

[leetcode-master/problems/数组理论基础.md at master · youngyangyang04/leetcode-master](https://github.com/youngyangyang04/leetcode-master/blob/master/problems/%E6%95%B0%E7%BB%84%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.md)

1. 数组：704.二分查找: 二分，一个方案：假设 target 始终在 [l,r) 之中，l = 左边界，r = 右边界（不可达），mid = (l+r)/2 这个idx
   一定偏左或者中，判断 mid 是否就是 target；判断 mid > target 则 r = mid；否则 l = mid + 1
2. 数组：27.移除元素：
   双指针：左边指针指向不等于 val 的元素，右边指针指向等于 val 的元素，交换它们
3. 数组：977.有序数组的平方
   // 双指针，因为这个平方后实际上是一个V型，所以可以从两边向中间遍历，把较大的放到新数组右边
   // 维护一个新队列，从右往左塞前面比较后的较大值
4. 209.长度最小的子数组：滑动窗口：维护一个滑动窗口，左指针和右指针，一个 sum 维护窗口内总和，一个 min_len 维护满足条件的窗口长度最小值
5. 59.螺旋矩阵II：模拟题，把控好规律和边界条件即可，四边缩小
6. 58 区间和：计算前缀和，用前缀和差值来计算区间和
7. 开发商购买土地
   二维前缀和，`data[x][y] = data[x-1][y] + data[x][y-1] - data[x-1][y-1]`，然后根据前缀和来算空间差值
8. 11 盛最多水的容器
   // 双指针，从两边开始遍历，每次移动较短的那根线，因为移动较长的那根线，面积只会更小
   // 如果移动较短的那根线，面积可能会更大
   // 所以每次移动较短的那根线，直到两根线相遇

# 链表

1. 203.移除链表元素
2. 707.设计链表：设计双向联表
3. 206.反转链表
   // 记录 preNode、curNode、nextNode
   // nextNode = curNode.Next
   // curNode.Next = preNode
   // preNode = curNode
   // curNode = nextNode
4. 24 两两交换链表中的节点
   // 引入一个临时头节点来简化操作
5. 160 链表相交：A，B 相互连接头尾，等待相等的时候，即相交或为nil
6. 142 环形链表II:
   // 快慢指针，慢指针一次一步，快指针一次两步，相交表示有环，然后分别从相交点和head开始走，再次相交点即为环开头
7. 19.删除链表的倒数第N个节点

# 哈希表

[哈希表理论基础 | 哈希表 | 哈希函数 | 哈希碰撞 | 代码随想录](https://programmercarl.com/%E5%93%88%E5%B8%8C%E8%A1%A8%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html#%E5%93%88%E5%B8%8C%E8%A1%A8)

1. 242.有效的字母异位词
2. 两个数组的交集
3. 快乐数：这道题是想说如何判断循环。判断循环的方法是：把每一次计算出的结果用集合记录下来，如果在计算过程中发现当前值之前出现过，则循环结束；否则就一直计算。
4. 第454题.四数相加II: 这道题是求所有满足条件的四元组，暴力是O(n^4)，使用哈希表可以优化到O(n^2)，key 是二元组的和，value
   是二元组的数量；
   把四数之和改为两数之和，分别两个 map 存两个数的和的数量，然后遍历两个 map，拿桶来算出现出现的组合次数
5. 第15题. 三数之和: 构造一个有序数组,让一个数不动，另外两个数来维护双指针，找到所有满足条件的三元组；固定 left，引入 mid 和
   right 双指针来探索边界，寻找另外两个数，注意的点是避免重复
6. 第18题. 四数之和:把四数之和改为两数之和，分别两个 map 存两个数的和的数量，然后遍历两个 map，拿桶来算出现出现的组合次数

# 字符串

1. 344.反转字符串
2. 541.反转字符串II
3. 151.翻转字符串里的单词
4. 28 实现 strStr()：
   KMP 算法求最长相等前后缀，原理：拿 needle 与自身匹配，求出每个位置的最大相等前后缀（每次当前位置不同则尝试到前一个最长相同前缀的位置，然后拿下一个数再和右侧比较）
   算出 next 数组后，拿 haystack 与 needle 匹配，每次不匹配则尝试回退 needle 的位置到前一个最长相同前缀处，然后拿下一个数再和右侧比较
5. 459.重复的子字符串: TODO

# 栈与队列

[栈与队列理论基础 | 栈 | 队列 | 容器适配器 | 代码随想录](https://programmercarl.com/%E6%A0%88%E4%B8%8E%E9%98%9F%E5%88%97%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html)

1. 232 用栈实现队列：用两个栈（输入栈和输出栈）来实现队列，输入栈用于入队，输出栈用于出队
2. 225 用队列实现栈：用一个队列，每次入队列时把最后一个元素放到最前面（把前面的元素出队列再入队列,顺序不变）
3. 20 有效的括号:维护一个栈，左边符号入栈，右边符号出栈左边符号并匹配
4. 1047 删除字符串中的所有相邻重复项:维护一个栈，来一个新的字符，如果与栈顶相同则弹出，否则入栈
5. 150 逆波兰表达式求值：维护一个栈，碰到数字入栈，碰到运算符出栈两个数字，计算结果入栈
6. 239 滑动窗口最大值：维护一个单调队列，队列内元素大小有序且入队元素顺序有序，Push 时比较队尾元素大小来 pop 尾部元素，Top
   计算队头最大/最小值，窗口缩小时判断队头是否被移出来 Pop 队头元素，这样始终保证队列内元素大小有序且入队元素顺序有序且元素都是窗口内的元素
7. 347 前 K 个高频元素：桶排序来做，先统计元素出现次数，按次数把元素放到桶中，然后按顺序遍历桶，把前 k 个元素放到结果中

# 二叉树

[二叉树理论基础篇 | 二叉树 | 遍历 | 存储方式](https://programmercarl.com/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html#%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E7%A7%8D%E7%B1%BB)

1. 二叉树的递归遍历: 递归算法（确定递归返回的参数和返回值，确定终止条件，确定单层递归需要处理的逻辑），中左右（前序），左右中（后序），左中右（中序）

2. 二叉树的递归遍历-栈实现
    - 前序遍历：pop 元素输出、然后尝试入栈右节点、左节点（因为出栈顺序是反着的）
    - 中序遍历: cur 始终指向待入栈的元素，cur 一直往左走，有元素就入栈，直到 nil，pop 回到 mid 节点，输出值，然后 cur 到右节点
      循环
    - 后序遍历：
        - 方案1，构造左右中，但注意重复：获取栈顶元素，如果没有孩子就说明是 mid
          元素，弹出并返回值；有右孩子则入右孩子，有左孩子则入左孩子（为了避免重复，需要入孩子后把对应指向设为 nil）
        - 方案2: 调整前序遍历的入栈顺序，最后把结果反转；中左右 -> 中右左 -> 结果反转 -> 左右中

3. 二叉树的层序遍历：使用队列，出队头输出值，把左右孩子入队列即可
4. 翻转二叉树：递归法：终止条件为当前节点为 nil；交换每个节点的左右子树，递归处理左右子树；栈法：入 root，pop
   栈顶元素，交换左右孩子，然后入左右孩子
5. 对称二叉树：核心点：拿左节点的左孩子和右节点的右孩子比较，拿左节点的右孩子和右节点的左孩子比较；两种思路，一种是递归，一种是使用
   queue 实现的层序遍历，每次从队列中取出两个节点进行比较，并放入对应的左右节点进行下一轮比较
6. 二叉树的最大深度：后序遍历&层序遍历，求高度；前序遍历求深度,携带深度信息到下一层
7. 二叉树的最小深度：前序遍历，但是注意判断叶子节点才算深度
8. 完全二叉树的节点个数:
    - 通过完全二叉树的定义来计算：一个完全二叉树可以拆解为 N 个满二叉树(`2**N-1`个节点)+最后叶子节点没有满(
      拆解为满二叉树+当前节点)；如果是满二叉树（左右高度相同）则返回`2**N-1`
      个节点；如果不是，则返回递归统计左右节点数之和+1;O(log n × log n)
    - 直接层序遍历：O(N)
9. 平衡二叉树:其实就是分求每个节点左右子树的最大高度，然后算高度差的绝对值是否 > 1
10. 二叉树的所有路径: dfs 前序遍历即可
11. 左叶子之和：dfs 前序遍历
12. 找树左下角的值：前序遍历，中节点判断当前深度，如果是世上最大则更新结果
13. 路径总和：前序遍历 中节点判断是否到叶子以及当前值，没有则判断左右子树
14. 从中序与后序遍历序列构造二叉树：利用中序（左根右）和后序（左右根）的特性；每次 pop 后序数组根节点，然后拆分中序左右节点来递归构造二叉树
15. 从前序与中序遍历序列构造二叉树: 根据前序遍历（根左右）和中序遍历（左根右），每次获取 根节点 拆分 左右节点构造二叉树
16. 最大二叉树:根据前序遍历来构造二叉树
17. 合并二叉树：合并当前节点，返回合并后节点; 当前两个节点都是 nil，返回 nil; 当前两个节点都不为空，则合并两者值，并分别对左右节点递归合并;如果其中一个为
    nil 返回另一个
18. 二叉搜索树中的搜索：考的定义，左子树所有节点值 < 根节点值 < 右子树所有节点值
19. 验证二叉搜索树：当前 root 大于左边的最大值 && 小于右边的最小值 并且 返回当前节点下的最小值和最大值 来用于上游判断
20. 530.二叉搜索树的最小绝对差:计算 root 和 左边最大值 和 右边最小值 差值，然后返回上游 最大值和最小值
21. 501.二叉搜索树中的众数: 中序遍历 元素值由小到大，此时维护 最大次数、当前值、当前值对应次数 以此来维护 众数列表即可
22. 236 二叉树的最近公共祖先:后序遍历：自己是否是 p/q + 左子树找到几个 + 右子树找到几个
23. 235 二叉搜索树的最近公共祖先:前序遍历：若当前节点值落在 p、q 之间或等于其中一个，它就是最近公共祖先；否则 p、q
    都在同一侧，按大小递归去左/右子树继续找。
24. 701.二叉搜索树中的插入操作：按照二叉搜索树的规则去遍历，遇到空节点就插入节点就可以了
25. 450.删除二叉搜索树中的节点：找到对应节点删除，然后修复该节点左右孩子的位置，例如：提拔 右节点 作为 root，把 左节点 放到
    右节点最左边(因为左节点一定是右边最小的)
26. 669 修剪二叉搜索树:
    ```
    还是递归解法，思路是 判断 root 是否在区间，满足就裁剪左右节点；不满足就直接把一边全裁了然后返回另一边
    1. root > high: return trimBST(root.Left, low, high)
    2. root < low: return trimBST(root.Right, low, high)
    3. root >= low && root <= high:
     - root.Left = trimBST(root.Left, low, high)
     - root.Right = trimBST(root.Right, low, high)
    ```
27. 108 将有序数组转换为二叉搜索树: 因为是有序的，找中间节点递归拆分即可
28. 538 把二叉搜索树转换为累加树：右中左遍历，维护一个 sum 总数即可（也是前一个节点的值）

# 回溯

1. 第77题 组合：
   // 组合：递归遍历，选择一个数，递归下一层，直到选够，实际上是用递归层级来动态模拟 for 循环
   // 剪枝：判断我还剩几个需要找，然后拿当前 N - 剩余个数 == 我前面可以枚举的个数，超过的及时遍历也无用（因为组合会被其他遍历到）
2. 216 组合总和III：组合遍历问题，还是用递归法，用每个层级来作为动态 for 循环，然后到达层级时判断是否满足目标，满足则记录答案；没有到最终层级时递归分发结果（可以剪枝）
3. 17 电话号码的字母组合: 每个 digit 作为每层进行遍历，然后每层遍历对应 digit 对应的 字母元素 作为当前层选择
4. 39 组合总和：
   // 思路：递归穷举法来无限组合，注意剪枝，先排序从小到大，然后每轮从头开始遍历组合，如果当前值 == target 则选取，否则如果小则继续，大则
   return;
   // 还有个点需要注意，不能有重复的组合，那每次把当前值遍历完，然后从下一个开始枚举组合，避免出现 223 232 这种情况，直接 223
   233 ...
5. 40 组合总和II
   // 注意几个点：1. 组合（枚举）；2. 每个数字只能出现一次（每层需要用下一个数，不能复用）；
   // 3. 结果的组合不能重复（需要考虑下 重复的原因 是 当前值可能匹配到了，下一个值可能和当前值相同，也会被匹配到导致重复，只需要每层相同的值枚举使用一次即可）
   // 满足三者条件即可
6. 131 分割回文串:切割问题，每次切割出一个回文子串，然后拿剩下的再去切割
7. 93 复原IP地址：
   // 其实就是组合问题 切割一个符合条件的值 然后剩下的再切割 直到整体符合预期则加入结果
   // 考虑两个点： 符合条件的（合法 ip 段）+ 剪枝（无法达成合法 ip 段或者ip 地址 就退出）
8. 78 子集：组合问题，直接枚举，只不过 子集 这个比较特殊，就是任何元素的组合都是属于子集的范畴，所以只要能组合出来就加到结果里
9. 90 子集II: 和子集的处理很像，但是关键点是避免重复的子集，先想下为啥会重复 1222 按之前的解法，会出现两个 (2,2)
   ，其实就是在于本层相同的处理的值处理一次需要跳过，那么先排序
10. 491 递增子序列：递归，但是需要一个注意点就是避免重复，由于是子序列，所以只需确保当前层的元素只使用过一次即可,其他元素就都不可能是重复的
11. 46 全排列:
    // 排列问题，主要点是有序的，顺序改变也会导致结果不同（每层循环从0开始利用每个数字），
    // 其次是每个数字不可重复使用（需要把当前排列中使用过的数字记录下来，避免重复使用）
12. 47 全排列 II
    // 和常规排列类似，需要从 0 开始，全部排列中每个值用一次（避免用超），每层相同的值只使用一次（避免重复排列）
    // 外层 seen 保障每个相同的 num 不会被用超；内层 seen 保障当前层中 每个相同的值只会被排列一次
13. 51 N皇后
    // 回溯算法，穷举法；如何定义维度：每层表示第几行，每层循环表示第几列，
    // 判定 ok 的条件，最终层数超过 n；
    // 满足条件，x，y 表示当前占用的行、列，其他人不可占用；对角线也不可占用

# 贪心算法

[关于贪心算法，你该了解这些！ | 贪心算法 | 局部最优 | 全局最优 | 代码随想录-全网最全算法数据结构刷题学习路线|图文+视频教程|免费开源](https://programmercarl.com/%E8%B4%AA%E5%BF%83%E7%AE%97%E6%B3%95%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html)

1. 455 分发饼干：贪心，饼干和胃口都从小到大排序，每次给最小胃口，最小能满足的，全局就是满足最多的
2. 376 摆动序列：如果差值和预期相同：则 +1 (至少有一个满足了)，注意当前一个差值为0（即前面有两个相同元素）时，当前差值可正可负；否则
   continue
3. 53 最大子序和：贪心， 每次开始必须是正的才能不拖累后面，否则就拿当前的值重新开始
4. 122 买卖股票的最佳时机 II: 贪心：每天都尝试买入，每天都尝试卖出，如果今天发现卖出亏了，那就昨天不要买；反之直接卖出赚差价；然后都尝试买入今天的
5. 55 跳跃游戏：贪心，遍历每个能走到的节点，更新并维护最大可走到的位置，判断是否可以走到终点
6. 45.跳跃游戏 II：贪心：每次算当前能到的最远的位置，以及路程中算下一次能到达的最远位置，如果当前到了本次最远位置还没到终点，那就新跳一此，更新下能到的最远位置
7. 1005 K次取反后最大化的数组和
   // 从小到大排序，遍历 nums，遇到负数就反转
   // 1. len(nums) >= k 没有遍历完 nums
   // 1. 没遍历完 nums
   // 1. 当前值为 负数 -- 直接反转
   // 2. 当前值为 正数 -- 在当前值和前一个值之间找一个较小的，判定最终反转，来消耗剩余的 k 次数
   // 2. 遍历完 nums 了，如果 k 还有剩余，那就拿最后一个 num 来计算最终反转状态来消耗剩余的 k 次数
   // 2. 计算总和
8. 134 加油站
   // 遍历数组过程中，维护两个值，
   // curTank：证明从 startIdx 走到数组末尾这一段不会断油。
   // totalTank：证明整体油量够，所以从末尾绕回 0，再走到 startIdx 前面那段也能兜住。
9. 860 柠檬水找零: 维护一个当前持久金钱的桶，计算当前找零是否可以满足即可，20（10+5）；10（5）
10. 452 用最少数量的箭引爆气球
    // 贪心：找最少的重叠区间，按 points[0] 从小到大排序，找有几个 截止窗口 重叠的区间，即最小的弓箭数
    // 能重叠在一起说明可以被一起击落
11. 435 无重叠区间:
    // 贪心：按 start_idx 从小到大排序，判断当前节点的 start_idx 和 上一个节点的 end_idx 如果重叠就去掉两个节点中 end_idx
    最小的那个
    // 注意遇到冲突了 不一定去掉当前，而是选择去掉两者中 end_idx 较小的，给后者留更多空间
12. 763 划分字母区间:
    // 本质还是合并区间，先遍历 s 统计每个字符出现的 idx 起止区间，然后统计每个字符的 start_idx 和 end_idx，然后合并所有重叠字符的
    起止区间，把不重叠区间长度记录下来即可
13. 738 从低位到高位循环遍历 n ，如果当前位 y < x，那么 x=x-1, 剩余低位 = 9

# 单调栈

1. 739 每日温度
   // 维护一个下标构成的单调非递增的栈，表示当前已经连续非升温的天气下标， [4,3,1] 遇到 2 -> [4,3,2]
   // 新来一个天气和栈顶比较，发现大于栈顶温度，则说明升温了，则弹出更新间隔天数
2. 496 下一个更大元素 I
   // 本质是找 nums2 中每个元素的 下一个 更大的元素
   // 那我遍历 nums2 ，维护一个单调非递增的 num 栈，和 nums2 中元素对应的下一个更大元素的 map
3. 503 下一个更大元素II
   // 维护一个非单调递增的栈，以及一个记录当前idx是否访问过的map
   // 循环遍历nums，
   // 1. 每个 num 尝试更新栈中元素
   // 2. 判断当前元素还在栈里就停止；没在栈里就继续
