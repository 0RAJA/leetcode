# Legacy 根目录练习存量区

这里保存早期直接放在 `leetcode/` 目录下的 Go 练习文件。

## 维护规则

- 该目录提交到 Git，用于保留历史记录。
- 不在这里继续新增或调试题解。
- 需要维护旧题时，迁移到标准独立 package，再补测试。
- 目录名前缀 `_legacy` 是有意设计：Go 的 `./...` 包发现会跳过该目录，避免多个 `func main()` 继续造成编译冲突。
